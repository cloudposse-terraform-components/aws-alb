components:
  terraform:
    dns-delegated:
      metadata:
        component: dns-delegated
      vars:
        zone_config:
          - subdomain: test
            zone_name: components.cptest.test-automation.app
        request_acm_certificate: true
